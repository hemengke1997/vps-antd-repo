{
  "scripts": {
    "dev": "npm run server",
    "start": "node ./start && vite",
    "prod": "npm run build && npm run server:prod",
    "build": "vite build",
    "server": "node ./server",
    "server:prod": "cross-env NODE_ENV=production node ./server"
  },
  "dependencies": {
    "antd": "^4.21.7",
    "compression": "^1.7.4",
    "cross-env": "^7.0.3",
    "express": "^4.18.1",
    "react": "^18.1.0",
    "react-dom": "^18.1.0",
    "sirv": "^2.0.2",
    "vite-plugin-ssr": "^0.4.9"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^2.0.0",
    "vite": "^3.0.2"
  }
}
